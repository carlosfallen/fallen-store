version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: fallen-todo_server_1
      APP_PORT: 5173

  server:
    image: carlosfallen/todo:2.0.3
    restart: on-failure
    init: true
    environment:
      - NODE_ENV=production
    ports:
      - "5173:5173"
      - "3001:3001"
    volumes:
      - ./data:/app/data
